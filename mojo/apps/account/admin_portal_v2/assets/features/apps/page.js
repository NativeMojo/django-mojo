import {placeholderPage} from '../../components/placeholder.js';

export function appsPage(ctx) {
  return placeholderPage(ctx, {
    eyebrow: 'Apps',
    title: 'Apps',
    copy: 'Applications, their deploys, and the address each one serves on.',
  });
}
