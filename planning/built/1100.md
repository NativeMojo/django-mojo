<!-- generated from maestro item 1100 — do not edit; the board item is the source of truth -->

# 1100 — Account app logit.warning sweep (8 conversions + 2 comment-only + 1 judgment)

Build-start snapshot (batch order #5 of 6 — the LARGEST). Builds on #1098's helper (`cc745132`).
Full approved plan + per-site table + challenge dispositions live on maestro Inbox item 1100.

## Landed context (verified at build-claim)
- Convert (8): handoff_group.py 128 + 305; disable.py 116; asyncjobs.py 68,80,86,109; login_event.py 92.
- Do-NOT-convert (2, add explanatory comments): handoff_group.py:492, redirect_allowlist.py **~766**
  (the `report_unlisted_destination` degraded path — line drifted from 443 after #1098's additions).
- Judgment (1): user.py:674 — STAYS a file log, bind the two interpolated values only.
- #1098's two sites (old redirect_allowlist `_warn_unusable_entry`, oauth refusal line) are ALREADY converted
  and deleted — nothing to skip there; do not re-touch.

## Helper binding
- asyncjobs / disable / login_event → import `incident.report_event_suppressed(details, key, ..., fail_open=True)`.
- The two handoff_group.py sites → reuse that module's OWN `_should_report`/`_report`/`_notice_key` (do not add a 2nd mechanism there).

## Load-bearing (from self-challenge)
- handoff_group:305 body says scheme/port/path DROPPED → entry covers MORE (host + all subdomains), not less.
- KEEP the `_CACHE[_WARNED_INERT]` one-shot at get_mode:128 (a test asserts it).
- disable.py:116 — BIND the currently-discarded exception.
- login_event:92 — move the report AFTER `event = cls.objects.create(...)`; key NOT per-user.
- asyncjobs — fix the never-interpolating printf at 109 AND 104; never echo api key / payload values.

## Verification
Builder runs ONLY its own targeted test modules (test_account.test_geoip_pushback_job / test_login_event /
test_disable_kill_switch, test_auth.handoff_group_token). Orchestrator owns the closing sweep. No `--full`.
