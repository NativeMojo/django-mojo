<!-- generated from maestro item 543 — do not edit; the board item is the source of truth -->
# testit server_settings — interleaved capture/restore persists stale overrides

Snapshot at build start (maestro-auto batch 543+544). Full workspec + plan +
challenge dispositions live on board item 543; this file is the local paper
trail per repo convention. Plan summary: key-scoped capture INSIDE the
exclusive hold + restore-by-subtraction under a conf mutex; writer-vs-writer
serialization lock; owner-aware read acquisition in server_lock; names-only
parsed-map drift check in the runner; ordering-pinning regression test in
tests/test_helpers/test_server_conf.py; docs (testit Overview, testit_guide,
CHANGELOG). Rider: harden tests/test_jobs/test_run_jobs_helper.py drain
assertion. See board item for the executable detail.
