<!-- generated from maestro item 179 — do not edit; the board item is the source of truth -->
# ShortLink.resolve() mutates before checking the destination

- **Kind**: bug
- **Date**: 2026-07-20
- **Found by**: post-build security review of item 151 (not caused by it — pre-existing)

Claimed 2026-07-26 (batch build: items 179, 56, 48 — shared baseline, single
final full-suite run). Full workspec + ## Plan live on board item 179
(NativeMojo board 11); this snapshot is the commit-time paper trail.

Plan summary: extract read-only `get_destination()` from `ShortLink.resolve()`;
reorder `resolve()` to compute the destination and return None on falsy BEFORE
the hit_count increment and metrics block. 4 unit tests + 1 REST test
(regression: `test_resolve_no_destination_no_hit`,
`test_resolve_no_destination_no_metric` fail on pre-fix main). Docs: both
shortlink READMEs + CHANGELOG (data-semantics note).
