<!-- generated from maestro item 1099 — do not edit; the board item is the source of truth -->

# 1099 — Test hygiene: dead cleanup_email_change, stale redirect_uri docstring, generalize the escapejs audit

Build-start snapshot (batch order #6 of 6, LAST). SMALL mechanical chore — resist scope growth.
Full approved plan lives on maestro Inbox item 1099.

## Three cleanups (verified present)
1. `tests/test_email/email_change.py:1602` `cleanup_email_change` (dead — runner has no teardown phase). Delete; record in setup docstring that setup owns cleanup.
2. `tests/test_oauth/redirect_uri.py:262` `test_redirect_uri_unusable_entries_are_skipped` — junk @268 has `myapp://callback`; drop it + fix the "non-http(s)" docstring; point at the file's existing custom-scheme tests (DEEP_ENTRY @337).
3. `tests/test_auth/bouncer_forms.py:951` `test_auth_base_script_literals_all_escapejs` → replace with discovery-based `test_shipped_templates_raw_text_interpolations_are_safe`; then fix contact.html (2 sites) + bouncer_challenge.html (5 sites); bouncer_decoy.html stays (its <style> colour is in the reviewed allowlist).

## Build note
Part C's audit test IS a fail-then-pass artifact: write it, run `-t test_auth.bouncer_forms` targeted to confirm it flags 7 sites, apply the template fixes, re-run targeted → green. That targeted run is permitted; do NOT run the full suite.
