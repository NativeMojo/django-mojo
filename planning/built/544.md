<!-- generated from maestro item 544 — do not edit; the board item is the source of truth -->
# docit_kb reconciliation cron — self-heal missed embed publishes

Snapshot at build start (maestro-auto batch 543+544; 543 landed first as
9c3403dd + 55559323). Full workspec + plan + challenge dispositions live on
board item 544. Plan summary: PageChunk.modified becomes a pipeline
watermark stamped with the page.modified SNAPSHOT read at embed start;
reconcile_stale_pages sweep with three self-clearing arms (stale w/
lookback+grace, never-chunked w/o lookback, null-embeddings gated on
provider availability), pre-slice exclusions + 1h in-flight recency bound,
hourly per-page recon idempotency keys; @schedule(minutes="*/15") dispatcher
gated on DOCIT_KB_RECONCILE_ENABLED, sweep-level 15-min idempotency key,
fully guarded. See board item for the executable detail.
