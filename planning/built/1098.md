<!-- generated from maestro item 1098 — do not edit; the board item is the source of truth -->

# 1098 — OAuth allowlist diagnostics: logit.warning → incident.report_event (Redis-suppressed)

Build-start snapshot (batch order #3 of 6 — the KEYSTONE). Builds on #1101 (`091e0d2e`) + #1102 (`8c5bc83f`).
Full approved plan + challenge dispositions live on maestro Inbox item 1098.

## Provides the shared helper #1103 and #1100 consume
`incident.report_event_suppressed(details, key, ..., window=3600, budget=None, fail_open=True)` in
`mojo/apps/incident/reporter.py`, exported from `mojo/apps/incident/__init__.py`, plus `notice_key`/`budget_key`.

## Current file structure (verified post-#1101/#1102)
- redirect_allowlist.py: `matches_allowlist` @266 (REWRITE — add `request=None, tenant=False`),
  `matchable_scheme` @298 (#1102's — PRESERVE), `_warn_unusable_entry` @320 (DELETE),
  `_WARNED_ENTRY`/`_WARN_CAP` @188-189 (DELETE), `_CACHE` @195 (keep resolver-cache only),
  `_split` @348 with #1101's dot-segment guard @363 (PRESERVE + keep the `candidate is None` early-out),
  `report_unlisted_destination` @484 (LEAVE — a test asserts its key shape; #1101 added a host fallback there).
- oauth.py: `_validate_redirect_uri` still in HEAD form (1102 did NOT touch it) — REWRITE into two sourced matches.

## Red-team amendments (load-bearing)
- `report_event_suppressed` gets `fail_open` param. Deployment-entry + #1100's internal sites → fail_open=True.
  The two attacker-amplifiable PUBLIC categories (`auth:oauth_redirect_refused`,
  `auth:redirect_allowlist_tenant_entry_unusable`) → fail_open=False (skip on Redis error, don't flood the DB).
- RETAIN both `if group:` and `if value:` guards; gate `group.pk` behind `if group_entries and matches_allowlist(...)`.
- Gate the unusable-entry report on `if unusable_count:` (clean match = ZERO Redis/DB cost).
- Tests clear BOTH notice_key AND budget_key in setup.
- Category names `auth:redirect_allowlist_*` (matcher is shared with AUTH_HANDOFF_ALLOWED_URLS).

## Verification
Builder runs ONLY `-t test_oauth.redirect_allowlist_incidents -t test_account.test_incident_suppression`.
Orchestrator owns the closing sweep. No `--full`.
